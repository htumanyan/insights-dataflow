<workflow-app name="shell-wf" xmlns="uri:oozie:workflow:0.5">
    <start to="at-add-part-customer"/>
	<action name="at-add-part-customer">
		<shell xmlns="uri:oozie:shell-action:0.1">
			<job-tracker>${jobTracker}</job-tracker>
			<name-node>${nameNode}</name-node>
			<configuration>
				<property>
					<name>mapred.job.queue.name</name>
					<value>${queueName}</value>
				</property>
			</configuration>
			<exec>${partitionScript}</exec>
			<argument>-d</argument>
			<argument>at</argument>
			<argument>-t</argument>
			<argument>export_customer_daily_metric_v_edw</argument>
			<argument>-h</argument>
			<argument>${namenode_host}</argument>
			<argument>-l</argument>
			<argument>/data/database/at/export_customer_daily_metric_v_edw/</argument>
			<file>${partitionPath}${partitionScript}#${partitionScript}</file>
			<capture-output/>
		</shell>
		<ok to="at-add-part-listing"/>
		<error to="failEmail"/>
	</action>
	<action name="at-add-part-listing">
		<shell xmlns="uri:oozie:shell-action:0.1">
			<job-tracker>${jobTracker}</job-tracker>
			<name-node>${nameNode}</name-node>
			<configuration>
				<property>
					<name>mapred.job.queue.name</name>
					<value>${queueName}</value>
				</property>
			</configuration>
			<exec>${partitionScript}</exec>
			<argument>-d</argument>
			<argument>at</argument>
			<argument>-t</argument>
			<argument>export_inv_listing_daily_metric_v_edw</argument>
			<argument>-h</argument>
			<argument>${namenode_host}</argument>
			<argument>-l</argument>
			<argument>/data/database/at/export_inv_listing_daily_metric_v_edw/</argument>
			<file>${partitionPath}${partitionScript}#${partitionScript}</file>
			<capture-output/>
		</shell>
		<ok to="endMain"/>
		<error to="failEmail"/>
	</action>
	<action name="failEmail">
		<email xmlns="uri:oozie:email-action:0.1">
			<to>${alertEmail}</to>
			<subject>oozie job failure</subject>
			<body>The workflow ${wf:id()} name ${wf:name()} path ${wf:appPath()} had issues and was killed.  The error message is: ${wf:errorMessage(wf:lastErrorNode())}</body>
		</email>
		<ok to="fail" />
		<error to="fail"/>
	</action>
	<kill name="fail">
		<message>Sqoop failed, error message[${wf:errorMessage(wf:lastErrorNode())}]</message>
	</kill>
	<end name="endMain"/>
</workflow-app>
